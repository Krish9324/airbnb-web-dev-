# Database Configuration
MONGO_URL=mongodb+srv://username:password@cluster.mongodb.net/wanderlust?retryWrites=true&w=majority

# Session Secret (Generate a random string for production)
SESSION_SECRET=your_super_secret_session_key_here

# Cloudinary Configuration (Optional - will use local storage if not provided)
CLOUD_NAME=your_cloudinary_cloud_name
CLOUD_API_KEY=your_cloudinary_api_key
CLOUD_API_SECRET=your_cloudinary_api_secret

# Port (Automatically set by Vercel)
PORT=8080

# Node Environment
NODE_ENV=production
